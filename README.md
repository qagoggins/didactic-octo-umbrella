1. /start has 2 settings -> eng or spanish, bot then only sends mark text that sets THAT language keywords
e.g. chose spanish, bot sends mark that sends custom keyboard of spanish buttons, that on click sends them text asking BOL in spanish



2. On 




require("dotenv").config();

const bot = new Telegraf(process.env.BOT_TOKEN);